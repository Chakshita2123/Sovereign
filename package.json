{
  "name": "sovereign-ssi",
  "version": "1.0.0",
  "description": "Sovereign — Personal Data Wallet (SSI Platform). Full-stack integration for PBE-I evaluation covering Node.js Lectures 1-24.",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "seed": "cd server && node utils/seedData.js",
    "dev": "npx concurrently --kill-others \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "start": "cd server && node utils/seedData.js && node server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
